export const ENTRY_WORKER_LINKS = {
    workers: {
        // href: `${environment.api.url}/api/entries/{entryId}/workers{?index,size,sort}`,
        href: '',
        templated: true,
    },
    worker: {
        // href: `${environment.api.url}/api/entries/{entryId}/workers/{workerId}`,
        href: '',
        templated: true,
    },
};
